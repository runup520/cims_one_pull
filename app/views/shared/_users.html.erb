<ul class="users">	
		<%@users.each do |user|%>
		<li><%= link_to user.name,user%>
			<% if current_user.admin? && !current_user?(user) %>
		    |<%= link_to "删除", user, method: :delete, data: { confirm: "确定删除该用户？" }%>
		    <% end %>
		</li>
	    <%end%>   
</ul>